# user_requirements.md 模板

本文件由 Claude 通过对话收集信息后自动创建和维护。**用户无需手动编辑此文件。**

Claude 在每个阶段开始前，若用户输入中信息不足，会在对话中主动提问收集，收集完成后将结果写入本文件对应章节，作为后续执行的约束依据。

**规则：**
- 本文件由 Claude 写入和维护，不由用户直接编辑
- Claude 绝不将本文件中的内容复制到 `idea_report.md` 或 `dev_log.md` 中
- 主文档只展示执行结果；本文件保存收集到的用户需求原文
- 用户可在对话中随时声明文档偏好（语言、详细程度等），Claude 会更新本文件对应字段

---

## 文件格式

```markdown
# User Requirements — {Topic}
> Created: {YYYY-MM-DD} | Project: {project_name}
> 由 Claude 根据对话内容自动填写，用户无需手动编辑。

---

## 阶段 A：方向探索需求
> Claude 在阶段 A 开始时通过对话收集，用于指导候选 idea 方向的生成。

### 对研究方向的理解
{从用户对话中提取的已有认知或背景知识}

### 希望侧重的方向
{从用户对话中提取的偏好方向，如轻量化/可解释性/跨域泛化等}

### 不希望涉及的方向
{从用户对话中提取的排除方向}

### 其他约束
{从用户对话中提取的其他限制}

### 文档偏好
{从用户对话中提取的文档语言、详细程度等偏好，默认：中文正文 + 英文章节标题}

---

## 阶段 C：实验设计需求
> Claude 在阶段 C 开始前通过对话收集，用于指导实验方案设计。

### 硬件条件
{从用户对话中提取的 GPU 型号、数量、显存上限}

### 时间约束
{从用户对话中提取的训练时长限制}

### 数据集偏好
{从用户对话中提取的数据集指定，或"由 Claude 推荐"}

### 对比实验侧重
{从用户对话中提取的实验重点}

### 其他约束
{从用户对话中提取的其他实验限制}

---

## 阶段 D：实现设计需求
> Claude 在阶段 D 开始前通过对话收集，用于指导 implementation.md 的生成。

### 编程语言
{从用户对话中提取，默认 Python}

### 框架偏好
{从用户对话中提取，默认 PyTorch}

### 代码风格要求
{从用户对话中提取的风格偏好}

### 基于已有项目
{从用户对话中提取的已有项目路径或链接，或"从头开始"}

### 其他要求
{从用户对话中提取的其他编码要求}
```

---

## Claude 收集需求的时机

### 阶段 A（方向探索）
若用户输入中未包含足够信息，Claude 在对话中一次性提问：

```
在开始检索前，想了解几点（可跳过任意一项）：
1. 你认为现有方法的核心问题是什么？
2. 你希望从哪个角度切入？（如：轻量化、可解释性、跨域泛化）
3. 有没有特别想参考的论文？
4. 有其他约束吗？（如：必须单卡运行）
```

若用户输入已足够详细，直接提取并写入，不再提问。

### 阶段 C（实验设计）
在开始设计实验前，Claude 在对话中提问：

```
在设计实验前，确认几点：
1. GPU 配置是什么？（型号 + 显存）
2. 单次训练有时间限制吗？
3. 数据集有偏好，还是让我根据领域惯例推荐？
4. 实验有特别侧重的部分吗？（如：重点验证某个消融）
```

### 阶段 D（实现设计）
在开始生成 implementation.md 前，Claude 在对话中提问：

```
在设计实现方案前，确认几点：
1. 框架用 PyTorch 还是其他？
2. 有没有基于已有开源项目？（如有，提供链接）
3. 代码有什么特殊要求吗？（如：支持多卡、需要 ONNX 导出）
```

